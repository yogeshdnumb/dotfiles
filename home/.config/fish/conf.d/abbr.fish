abbr -a pac "sudo pacman -S"
abbr -a par "sudo pacman -R"
abbr -a paf "sudo pacman -F"

abbr -a rf "rm -rf"
abbr -a cp "cp -r"
abbr -a md "mkdir -p"

abbr -a gc "git clone --depth=1"
abbr -a ga "git add ."
abbr -a gm "git commit -m"
abbr -a gp "git push"
abbr -a gs "git status"

abbr -a vi "nvim"
abbr -a code "vscodium"
abbr -a xp "chmod +x"
abbr -a py "python"
abbr -a bpy "bpython"

